# Procedural rear-tree branch child-partition family 001

Status: **DRAFT / DERIVED REGION PARTITIONS / NO RIGGING ADOPTION**

## Repeated pattern

Organic Form now source-owns five primary east-rear branches and exact root-centered flex metadata for all five. The generated mesh repeats the same child construction for every branch: two tapered branch-segment regions plus a matching four-blade `<branch>-leaves` cluster. Rigging PR #14 independently hand-selects that exact construction for `north-top` as 52 vertices / 72 triangles while leaving 338 generated vertices fixed.

Procedural therefore owns one smaller reusable task: derive those exact generated-region partitions deterministically for every primary branch so a future receiver does not hand-author region-prefix lists branch by branch.

## Bounded family

Contract: `axm.nature-branch-child-partition-family/v0.1`.

Pinned Organic owner:

- PR #8 head `fdc9d2b6ee729728551e22fd3eafa23ad60b6c7a`;
- source digest `178cd8cfb1a859bff411f60e13154109528062cf0ad2384b343d406cc0cc9d61`;
- generated mesh digest `d7fc5deaa1c12d1d8c7d7b6dc95bf1e8544ce26140ee2e4a7d2c67a2c4133e48`;
- exact mesh size 390 vertices / 570 triangles.

The family emits five materially different partitions: `south-low`, `north-low`, `east-mid`, `west-high`, and `north-top`. Each partition is derived from exact generated region identities, exact branch root/flex metadata, and exact leaf-cluster membership. No source point, radius, blade, region, joint, weight, or pose is authored here.

The current construction yields 52 selected vertices / 72 triangles and 338 fixed vertices per branch. The five selected vertex-index sets must be pairwise disjoint, all five partition identities must be distinct, and reversing input declaration order must reproduce the exact canonical family identity.

## Failure bounds

Fail closed on Organic owner-head drift, source/blob drift, builder/blob drift, generated-mesh drift, duplicate or unknown branch selectors, flex-status promotion, flex-center drift, generated-region identity drift, or any attempt to enable automatic Rigging adoption.

## Authority boundary

This is a deterministic generated-region selection family only. Organic Form retains source form and flex metadata. Rigging retains socket, hierarchy, child ownership, deformation, range and weights. Animation retains motion timing. VFX retains response semantics. Runtime retains representation/device acceptance. Map/Environment and Art/Visual QA retain scene and perceptual acceptance.

No partition is automatically adopted. No deformation PASS transfers from one branch to another. `axm-create-me` remains coordination-only. Truth, Agency / non-domination, Continuity, and Wisdom before speed remain the merge gate.
