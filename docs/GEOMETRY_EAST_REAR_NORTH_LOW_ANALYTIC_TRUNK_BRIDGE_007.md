# Geometry — north-low analytic trunk-surface bridge candidate 007

## Why this exists

The previous Geometry successor exposed one structurally clean eight-edge branch-side open boundary at the exact `north-low` owner transition but intentionally held the trunk opening, bridge and connected junction. Organic Form then supplied an exact neutral transition exit frame at that same owner boundary, including the local trunk tangent, centerline point/radius, radial direction and authored branch tangent.

This pass consumes that new handoff without inventing a source reshape or silently promoting the detached Rigging socket.

## Exact owners

- Geometry predecessor: `a7164ce18e309c3cd548c89380639bdfa2b46ebf` — true two-parent continuity merge of prior Geometry head and Organic exit-frame head.
- Organic exit-frame owner: `7f3b937b440e9870d07f7c356c5a3cbb779d0cd7`.
- Organic exit-frame observer blob: `406c169963c34aed394e269f0dff2c91bb035cb8`.
- Current detached Rigging owner retained read-only: `69640e558f0c1ac59d4d0e3155676e0967a03d04`.
- Source digest remains `178cd8cfb1a859bff411f60e13154109528062cf0ad2384b343d406cc0cc9d61`.

## Bounded construction

The existing eight-vertex branch-side ring is reused byte-for-value through its predecessor builder. Each ring vertex is projected onto the exact authored `mid->upper` tapered trunk analytic envelope while preserving that vertex's local axial coordinate along the trunk segment. The resulting eight projected points form a candidate trunk-surface loop.

Geometry then pairs branch ring vertex `i` with trunk loop vertex `i` and builds an eight-quad / sixteen-triangle annular bridge patch. The branch stub plus bridge is evaluated as one diagnostic candidate, but the indexed trunk mesh itself is not cut, rewritten or welded.

Reusable rule:

`ANALYTIC_TRUNK_ENVELOPE_PROJECTION_CAN_DEFINE_MATCHED_BOUNDARY_LOOP_AND_BRIDGE_PATCH__INDEXED_TRUNK_CUT_AND_CONNECTED_JUNCTION_REMAIN_SEPARATE_GATES`

## Structural result

Scoped result:

`PASS_NORTH_LOW_ANALYTIC_TRUNK_SURFACE_LOOP_AND_BRIDGE_PATCH__HOLD_INDEXED_TRUNK_CUT_CONNECTED_JUNCTION`

Expected bridge annulus:

- 16 vertices;
- 16 triangles;
- 32 undirected edges;
- one triangle component;
- two simple eight-edge boundary cycles;
- Euler characteristic 0;
- zero non-manifold edges, winding conflicts, degenerates or isolated vertices.

Expected branch-stub + bridge candidate:

- 25 vertices;
- 40 triangles;
- 64 undirected edges;
- one triangle component;
- one remaining simple eight-edge boundary on the analytic trunk surface;
- Euler characteristic 1;
- zero non-manifold edges, winding conflicts, degenerates or isolated vertices.

Every projected trunk-loop point must remain on the exact local tapered trunk envelope within the verifier tolerance, every projection must stay inside the exact `mid->upper` segment interval, and every bridge span must remain strictly positive. The prior branch ring must agree with the exact Organic exit center/radius/tangent handoff.

## Authority boundary

This evidence is intentionally one step short of a connected production junction. It does **not** prove or perform:

- an indexed cut/opening in the current trunk mesh;
- a weld, boolean union or remesh into the current receiver;
- shared branch/trunk vertex identity;
- bridge self-intersection freedom against the full receiver;
- source/default adoption;
- Rigging hierarchy, weights or deformation quality;
- Animation/VFX behavior;
- Technical-Art target-host transport;
- Runtime/device/collision/gameplay acceptance;
- CANON, production readiness, game readiness or Geometry mastery.

The next Geometry gate, if justified by receiver evidence, is an **indexed trunk-cut successor** that preserves the proven analytic loop/bridge relation and then independently revalidates connectivity, intersections and downstream owners. Until then this diagnostic patch remains non-adopted evidence.
