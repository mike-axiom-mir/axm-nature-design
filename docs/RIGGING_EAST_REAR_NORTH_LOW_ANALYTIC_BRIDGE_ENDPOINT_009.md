# Rigging — north-low analytic bridge endpoint gate 009

## Bounded question

Geometry PR #18 now owns one exact analytic `north-low` bridge candidate: an 8-vertex branch-side transition boundary, an 8-vertex projected trunk-side boundary, and a 16-triangle annular patch between them. Geometry explicitly holds the indexed trunk cut / connected junction and production deformation questions.

Rigging therefore asks only:

> Can those exact two Geometry-owned boundaries be assigned explicit diagnostic deformation ownership without changing the existing north-low socket or inventing production skin weights?

## Exact provenance

- Rigging predecessor: `69640e558f0c1ac59d4d0e3155676e0967a03d04`;
- predecessor attachment-gate module blob: `de00694e103b975f9a25a72a50054821b469b528`;
- Geometry bridge donor: `14d05fdabc943376c231308001d00eb87dc23430`;
- Geometry bridge module blob: `47ba57110f43b0f3f1f29582b46a90505fa4515c`;
- Geometry bridge contract blob: `047daa7d5764f53cb42aa14b8bec82f3150bd809`;
- exact Organic/source and migrated-receiver identities remain inherited from the predecessor and Geometry donor and are revalidated by their own evaluators;
- exact current Organic evidence dependency `rear_tree_flex_interaction_classification.py` is pinned byte-for-byte to blob `be31e22283d43dcb4e6e8bda870760fa67ab1628` from the current Organic base.

No source position, topology, existing pivot, existing axis, diagnostic range, Animation clip, Technical-Art receiver, Runtime representation or UC product code is rewritten.

## Integration continuity repair

After the first bounded Rigging evidence head was green, the ordinary Organic east-rear evidence workflow exposed a separate branch-integration defect: `tools/build_rear_tree.py` imports `rear_tree_flex_interaction_classification.py`, but that source-owned observer was absent from the long-lived stacked Rigging branch even though it exists on the current Organic base.

The full receiving unit suite still passed 158 tests before that evidence-build failure, so the defect was not interpreted as a deformation failure. Rigging restored the missing observer from the current Organic base without altering its semantics. The first restoration commit `5ac065f6fe9dee4ac78fdb94e1a26db6a3d44318` was immediately found not to be byte-identical and was superseded by `e972caaf9fb6482659e6eca43cf7aa9a26be81f4`, which restores the exact owner blob `be31e22283d43dcb4e6e8bda870760fa67ab1628`.

The Rigging workflow now pins that exact Organic-owned blob and retains it only as an evidence dependency. This repair changes no Rigging endpoint math, Geometry bridge geometry, source JSON, owner flex semantics, motion range, weights, Animation or Runtime behavior.

## Diagnostic endpoint constraint

`ANALYTIC_BRIDGE_TWO_BOUNDARY_DIAGNOSTIC_PIN`

- branch boundary: 8 vertices, follows the existing `north-low` child socket with diagnostic endpoint weight `1.0`;
- analytic trunk boundary: 8 vertices, remains receiver-pinned with diagnostic endpoint weight `0.0`;
- bridge interior vertices: `0`.

The `1.0 / 0.0` values describe only endpoint ownership for this analytic diagnostic bridge. They are **not production skin weights** and do not prescribe a future connected-junction blend.

## Motion evidence

Representative child poses are `-5 / -2.5 / 0 / +2.5 / +5°` inside the already-owned `[-5,+5]°` Rigging diagnostic interval.

For each retained pose the verifier checks:

- exact branch-boundary agreement with the existing child socket transform;
- zero trunk-boundary receiver drift;
- rigid preservation of branch-boundary edge lengths;
- identity preservation of trunk-boundary edge lengths;
- all 16 representative bridge triangles remain non-degenerate;
- all 8 paired branch-to-trunk spans remain non-zero.

The endpoint map itself is continuous for every real command in the existing child interval because the branch boundary is one continuous rigid Rodrigues transform and the trunk boundary is identity-mapped. Representative triangle-area/span checks are **sampled only**; they do not prove continuous bridge foldover, collision or self-intersection freedom.

Two negative controls keep the ownership choice discriminating: fixing the branch boundary at the +5° child witness must create measurable mismatch, and making the trunk boundary follow the child must create measurable receiver drift.

## Authority boundary

This result does not establish an indexed trunk cut, welded/connected branch-trunk topology, production skinning/blending, botanical mechanics, stress/strength validity, biological ROM, continuous collision/foldover freedom, Animation timing/interpolation/playback, Technical-Art target-host adoption, Runtime/controller/device/performance acceptance, Art/Visual-QA acceptance, CANON or production readiness.

Organic keeps source/form authority. Geometry keeps topology/bridge authority. Rigging owns only the explicit diagnostic endpoint constraint and its bounded pose evidence. `axm-create-me` remains coordination-only.

The AXM roots remain the gate: **Truth; Agency / non-domination; Continuity; Wisdom before speed**.
