# East-rear primary-branch Animation family 002

## Purpose

This successor extends the existing single `north-top` Animation diagnostic into one reusable **family method** over the exact five-socket Rigging owner evidence at `898529f602893c8f6be179bd3e9b6821fc099904`.

It does not retime, widen or reinterpret Rigging's `-5..+5°` diagnostic interval. The interval remains `RIGGING_VERIFICATION_PROBE_ONLY_NOT_SOURCE_OR_BIOLOGICAL_ROM`.

## Bounded method

Contract: `axm.nature-animation-root-socket-diagnostic-pulse-family/v0.2`.

The canonical branches are:

1. `south-low`
2. `north-low`
3. `east-mid`
4. `west-high`
5. `north-top`

Each branch receives its own independent sampled diagnostic clip:

- duration `1.0 s`;
- sampled review cadence `40 Hz`;
- `41` endpoint-inclusive samples / `40` visible repeating samples;
- `angle = 5 * sin(2*pi*t)^3`;
- exact landmarks `0° -> +5° -> 0° -> -5° -> 0°`.

The five clips deliberately run **one branch at a time**. This avoids converting Rigging's independent structural proofs into an unsupported simultaneous multi-branch claim.

## Exact-owner consumption

CI fetches exact Rigging PR #14 head `898529f602893c8f6be179bd3e9b6821fc099904` into an isolated worktree and executes its own five-branch family evaluator before Animation observation begins. Animation then consumes the owner pivots, source-derived axes, exact 52-vertex child partitions and retained witness poses directly from that exact worktree.

No Rigging source is copied into Animation and no owner authority is transferred.

## Motion evidence

For each independent branch clip the observer applies the exact owner Rodrigues rotation to the owner-selected child vertices and verifies all 41 sampled states:

- 338 non-child vertices remain exact;
- the owner pivot remains exact;
- child pairwise distances remain rigid;
- axis projection remains invariant;
- endpoint returns exactly to neutral;
- visible loop-wrap step matches the authored final adjacent step;
- the curve remains bidirectionally antisymmetric;
- Animation replay reproduces Rigging's retained `-5/-2.5/0/+2.5/+5°` witness metrics.

A deterministic SVG plots actual maximum selected-vertex displacement through time for all five independent clips. It is sampled review evidence, not target-engine playback.

## Fail-closed boundaries

The verifier rejects Rigging-head drift, retiming, sample-rate drift, amplitude widening, wind semantics, source/biological ROM promotion, simultaneous-motion acceptance, Runtime-controller acceptance and gameplay acceptance.

## Ownership / nonclaims

Organic owns source form/flex declarations. Geometry owns migrated topology. Procedural owns its deterministic selection handoff. Rigging owns pivot/axis/child partitions and the diagnostic articulation interval. Animation owns only this sampled timing/motion method and its evidence. VFX owns wind-response semantics. Technical Art owns target-host transport. Runtime owns controller/state-machine/device performance. Art Direction and independent Visual QA own aesthetic acceptance.

A PASS here does **not** establish natural vegetation timing, wind/biomechanics, simultaneous multi-branch behavior, target-engine playback, wall-clock/display cadence, Runtime controller/state-machine behavior, gameplay/input/collision, target-device performance, Art/QA acceptance, CANON or production readiness.
