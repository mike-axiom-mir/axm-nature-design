# Procedural rear-tree root flex-zone review family 001

Status: **DRAFT / REVIEW-ONLY CANDIDATES / NO SOURCE AUTHORIZATION**

## Repeated pattern

The exact east-rear Organic source already repeats the same branch-flex declaration shape at four primary branch roots:

- declaration center is exactly the authored branch root;
- status remains `DECLARED_NOT_DEFORMATION_TESTED`;
- owner-authored branch-flex radius classes are exactly `0.12 m` and `0.14 m`.

Organic evidence head `b681b8beb0f91c6cedb6067aa8a79ae9feea6482` reports five primary branch roots but exact root-centered flex coverage of only `4 / 5`; `north-top` is the only missing root. Procedural therefore automates the repeated declaration construction and review packaging only. It does not invent a third radius class or choose a production value.

## Bounded family

Contract: `axm.nature-root-flex-zone-review-candidate-family/v0.1`.

The family retains three materially different source states:

1. exact owner baseline: `4 / 5` coverage, `HOLD_BRANCH_ROOT_FLEX_ZONE_COVERAGE`;
2. review-only `north-top` candidate using exact owner-authored `0.12 m` class;
3. review-only `north-top` candidate using exact owner-authored `0.14 m` class.

The reusable builder also reconstructs all four existing owner declarations exactly before creating either review candidate. This tests the mechanism against four already-authored examples instead of treating the missing root as one lucky case.

## Failure bounds

Fail closed on exact Organic head/blob/source-digest drift, a radius outside the two owner-authored classes, off-root center, status promotion, duplicate declaration at an already-covered root, or any attempt to enable automatic source adoption / deformation semantics.

Neither candidate is a source edit or a recommendation. A candidate that closes the metadata-coverage HOLD still remains explicitly deformation-untested.

## Authority boundary

Organic Form retains source metadata/form authority. Rigging and VFX retain deformation semantics and response authority. Animation retains time-domain motion. Runtime retains representation/device acceptance. Environment, Art Direction and Visual QA retain receiving-scene and perceptual acceptance.

No candidate is adopted automatically. `axm-create-me` remains coordination-only. The four AXM roots remain the merge gate.
