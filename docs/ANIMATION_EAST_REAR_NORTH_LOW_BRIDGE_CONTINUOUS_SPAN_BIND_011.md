# Animation — north-low continuous bridge-span bind 011

Owner: 3D Animation & Motion Specialist

Status target: `PASS_NATURE_NORTH_LOW_ANIMATION_CURVE_INSIDE_CURRENT_BRIDGE_CONTINUOUS_SPAN_DOMAIN`

This bounded successor does not change the existing north-low motion. It preserves the exact 1.0 s / 40 Hz / 41-key `sin(2*pi*t)^3` child track from Animation semantic owner `5cacd61e22433b0c33f29111827283b81cc0ba0d` and the later AnimationPlayer target-loop lane predecessor `20d04d70845c51455cedfb8541e2eb4e4784b433`.

The fresh owner handoff is current Nature Rigging head `efe99261459858636dbe65b16cbe1d5ad2b93a56`. Rigging now proves that all eight exact analytic branch/trunk paired spans remain continuously non-zero for every real north-low child command in the closed interval `[-5,+5]°`. Animation's exact scalar curve also lives in the closed interval `[-5,+5]°`; it reaches both endpoints but does not exceed them.

The evidence path must therefore do two different jobs:

1. replay all 41 authored Animation child angles over the current Rigging-owned analytic bridge endpoints and compare direct Rodrigues motion with Rigging's closed form;
2. prove the all-time Animation range analytically, then compose that range only with the exact Rigging continuous paired-span certificate.

The first job is sampled actual endpoint motion. The second is the continuous claim. Sample density is not used to pretend continuity.

The current Rigging socket identity must match the frozen Animation socket identity by branch, pivot, source-derived axis and diagnostic interval. A verifier-only `+5.01°` widening and a verifier-only socket-pivot mismatch must both fail closed.

## Boundary

This result does not prove continuous bridge-triangle orientation/nondegeneracy, foldover/collision/self-intersection clearance, connected branch/trunk topology, production skinning, physical wind, target-host playback for the current Rigging pair, wall-clock 40 Hz delivery, Runtime controller/state-machine/input/device behavior, gameplay collision/physics, Art/QA acceptance, CANON, game readiness or production readiness.

Rigging retains bridge endpoint and continuous paired-span authority. Animation retains timing/curve authority. Technical Art retains target-host adoption authority. Runtime retains controller/device/performance authority.
