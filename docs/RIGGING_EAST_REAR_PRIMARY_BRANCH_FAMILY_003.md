# Rigging east-rear primary-branch root-socket family 003

## Bounded question

The exact east-rear Nature source now carries five source-declared primary branch root flex zones, and Procedural PR #4 independently proved five deterministic, pairwise-disjoint generated child selections on the exact Geometry PR #9 receiver. Only `north-top` had Rigging-owned articulation evidence.

This successor asks one Rigging-owned question only: can the same already-proven diagnostic rigid-child articulation policy be independently re-executed at all five exact source root centers on the exact migrated receiver without changing source form, topology, or sibling ownership?

## Exact lineage

- Organic source owner PR #8: `fdc9d2b6ee729728551e22fd3eafa23ad60b6c7a`;
- source digest: `178cd8cfb1a859bff411f60e13154109528062cf0ad2384b343d406cc0cc9d61`;
- Geometry receiver PR #9: `9b451ba1f65281f550a6754e18574f7ab2951e28`;
- migrated receiver mesh: `aa9d450a78fef722672ea9af0f9aca98b4c1a0ca3705661784f5f61f3e9b6a31`;
- Rigging predecessor PR #14: `f792d2369675d532be478e17c7a07f441d817c7c`;
- Procedural selection donor PR #4: `342385c867050dc8e8f7312c63235a68bdd92d3d`;
- Procedural Geometry-rebind contract blob: `73e38d08f426efb742e1d66b992ede04737ed576`.

The Procedural handoff is selection evidence only. Its `automatic_rigging_adoption=false` boundary is preserved. Rigging independently retests articulation and does not relabel a Procedural PASS.

## Rigging family

The exact primary branches are:

- `south-low`, owner flex radius `0.14 m`;
- `north-low`, owner flex radius `0.14 m`;
- `east-mid`, owner flex radius `0.12 m`;
- `west-high`, owner flex radius `0.12 m`;
- `north-top`, owner flex radius `0.12 m`.

Every owner flex declaration must remain `DECLARED_NOT_DEFORMATION_TESTED` in source. The Rigging probe does not rewrite that source truth state.

For each branch independently, Rigging:

1. uses the exact branch root / flex center as the pivot;
2. derives a bend-plane axis from `local trunk tangent × first branch tangent`;
3. selects exactly two branch regions plus that branch's four leaf-blade regions;
4. requires `52` selected vertices / `72` selected triangles / `338` fixed vertices;
5. evaluates representative angles `-5 / -2.5 / 0 / +2.5 / +5°`;
6. preserves the fixed receiver, pivot, child pairwise distances, and projection onto the rotation axis;
7. re-runs the existing `north-top` predecessor and requires the generalized family to reproduce it.

The five child vertex selections must remain pairwise disjoint.

## Continuous structural claim

Each branch is tested as an **independent single-branch probe**. Within the declared `[-5,+5]°` diagnostic interval, selected vertices use one Rodrigues rigid transform about that branch's exact source-owned pivot and source-derived axis while the unselected receiver is identity-mapped. Therefore the rigid-transform invariants are continuous for that branch throughout the interval.

That analytical statement does **not** prove simultaneous multi-branch motion, collision/self-intersection freedom, surface attachment, blended weights, or biological range of motion.

## Truth boundary

A PASS here is Rigging structural evidence only. It does not establish source/biological ROM, production skin weighting, flexible attachment quality, stress or botanical validity, wind/VFX behavior, Animation timing/interpolation/playback, Technical-Art target-host adoption, Runtime/controller/device behavior, gameplay/physics, final visual acceptance, CANON, production readiness, or Rigging mastery.

Organic keeps source/form authority. Geometry keeps topology authority. Procedural keeps deterministic selection-family authority. Rigging owns only this independent articulation/deformation evidence. `axm-create-me` remains coordination-only; Truth, Agency / non-domination, Continuity, and Wisdom before speed remain the gate.
