# Runtime east-rear current Animation -> compressed shader-driver rebind

This bounded Runtime successor answers one question only: can the exact current five-socket Animation authored sample family drive the existing pass-54 compressed shader representation while preserving the current Animation target-position packets as the control oracle?

The current semantic owner is Animation head `d9899fc42afab432d0da6f9ecc3568dc90b7acbf`, whose exact target-replay builder derives current packets from Rigging owner `b4b480b415047fea90b4740f7702ced0dba9142d`. The older Technical-Art receiver is reused only as a transport substrate. Its predecessor pose oracle is not adopted as current motion authority.

The comparison keeps the exact imported compressed 390-vertex receiver for the shader candidate. For each of the 40 visible authored samples, the control path writes the current Animation packet into the exact `[110,370)` mutable position window while the candidate updates only the shared shader driver scalar. Nine fixed-view sample pairs are retained for raster comparison.

Budget accounting is semantic representation accounting, not measured GPU command traffic. The 3,120-byte current dynamic packet versus the 4-byte driver value is counted per authored visible sample, and the 3,120-byte imported compressed position store is compared with the 4,680-byte mutable control store. Additional vertex-shader range tests and rotation math remain an explicit tradeoff.

This proof does not claim AnimationPlayer natural playback, Runtime controller/state-machine behavior, newer Rigging hierarchy `6cf64925...`, physical wind, target-device frame-time or energy improvement, normal/tangent deformation correctness, Art/Visual-QA acceptance, CANON or production readiness.
