# Rigging — north-low attachment representation gate 008

## Scoped result

`PASS_NORTH_LOW_DETACHED_CHILD_SOCKET_ATTACHMENT_REPRESENTATION_GATE_DIAGNOSTIC`

This successor keeps the exact existing `north-low` socket and parent-influence exclusion unchanged, but binds them to Geometry PR #18's fresh indexed-topology classification. Geometry reports the exact child as 52 selected vertices / 72 triangles across six indexed components, with **zero indexed vertices shared with the trunk**. The root branch segment is itself a closed capped shell.

Rigging therefore records the current representation as `DETACHED_DIAGNOSTIC_CHILD_SOCKET_ONLY`. The socket transform remains useful diagnostic evidence, but it must not be relabelled as welded branch/trunk skinning, connected surface continuity, or a production attachment.

## Exact identities

- Rigging predecessor: `975931b11555d156e04e2ab12e9756fc6c9598a3`;
- Geometry attachment donor: `d32a41558910d78595042fb638a785714f825806`;
- Geometry donor contract blob: `127502ae50c7b442c88edd8454c9a74ba053da71`;
- Geometry donor observer blob: `279bd95a80208f346bd654e300e4b02aff34cf47`;
- Organic source owner: `fdc9d2b6ee729728551e22fd3eafa23ad60b6c7a`;
- source digest: `178cd8cfb1a859bff411f60e13154109528062cf0ad2384b343d406cc0cc9d61`;
- migrated receiver digest: `aa9d450a78fef722672ea9af0f9aca98b4c1a0ca3705661784f5f61f3e9b6a31`.

## Preserved pose proof

The previous nine parent×child witnesses are re-executed through the exact predecessor: parent `-2.5 / 0 / +2.5°` and child `-5 / 0 / +5°`. Parent-command leak remains zero within the existing tolerance and the child remains rigid. The continuous transform-domain certificate remains valid **only as a diagnostic transform field**. No connected-surface continuity is inferred from it.

## Fail-closed boundary

The receiver rejects donor/head drift, any change to the exact Geometry contract, any promotion to connected branch/trunk attachment, production skinning or production parent weighting, and any attempted Animation, Technical-Art or Runtime acceptance.

This result changes no source, Geometry bytes, pivot, axis, angle domain, weights, Animation clip, VFX behavior, target-host representation, Runtime behavior, Art/QA decision, CANON or production state.

The four AXM roots remain the merge gate: **Truth; Agency / non-domination; Continuity; Wisdom before speed**.
