# North-low parent-exclusion temporal rebind

This bounded Animation pass consumes the current Rigging north-low attachment representation without changing source form, Geometry, pivots, axes, selected child identity, diagnostic intervals, or Rigging's parent-influence gate.

## Frozen child timing

The earlier Nature Animation timing is preserved for `north-low`:

- duration: `1.0 s`;
- sampled cadence: `40 Hz`;
- endpoint-inclusive samples: `41`;
- visible repeat samples: `40`;
- normalized curve: `sin(2*pi*t)^3`;
- child landmarks: `0 -> -5 -> 0 -> +5 -> 0 deg`.

The sign is the existing north-low polarity from the five-socket shared driver. No natural-wind or biological-motion meaning is added.

## Fresh temporal question

Current Rigging classifies north-low as `DETACHED_DIAGNOSTIC_CHILD_SOCKET_ONLY` and holds upper-trunk parent influence at diagnostic weight `0.0`. Animation therefore adds a same-phase parent-command stress track `0 -> +2.5 -> 0 -> -2.5 -> 0 deg` while replaying the unchanged child timing.

At every sampled time the accepted child pose must be byte-semantically equivalent to the child-only transform. A counterfactual that actually inherits the parent frame is computed separately and must differ materially, proving the observer can detect the forbidden hierarchy transfer.

The parent stress channel is a command-only diagnostic. This pass does not define or animate an upper-trunk deformation receiver.

## Fail-closed boundary

The verifier rejects retiming, cadence changes, child or parent amplitude widening, inherited-parent motion, connected-attachment promotion, production-skinning promotion, wind/biological-ROM promotion, target-host promotion, Runtime-controller promotion, and gameplay promotion.

A PASS is sampled source-mesh Animation evidence only. It does not establish connected branch/trunk topology, production skinning, physical wind, continuous collision/surface continuity, Godot or other target-host playback, wall-clock 40 Hz delivery, Runtime controller/state-machine/input/device behavior, physics/gameplay, Art Direction or Visual QA acceptance, CANON, or production/game readiness.

The four AXM roots remain the gate: Truth; Agency / non-domination; Continuity; Wisdom before speed.
