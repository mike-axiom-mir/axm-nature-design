# Organic Form — east-rear neutral transition exit frames 008

## Why this exists

The previous Organic pass measured how far each authored primary branch remains fully supported by the authored neutral trunk radial envelope before its full tapered radius first reaches that envelope boundary. Procedural Design has since canonicalized those five scalar transition windows for deterministic consumers. Re-copying or extending those scalar values would duplicate an owned lane.

Geometry PR #18 and Rigging PR #14 also keep the current `north-low` receiver explicitly detached: spatial support and a diagnostic socket do not prove an indexed welded branch/trunk junction. The smallest new Organic-owned deformation-readiness contribution is therefore the **local authored source-form frame at each already-measured transition exit**, without choosing topology, hierarchy, skinning or deformation policy.

This gives a future Geometry-owned junction experiment exact source-space center/radius/tangent evidence while preserving the current disconnected-topology truth.

## Exact source identity retained

- Organic source/form owner: `fdc9d2b6ee729728551e22fd3eafa23ad60b6c7a`;
- source path: `examples/east_rear_tree_neutral_001.json`;
- source Git blob: `fb12b759e1abfd0455bf46fd39a0eba27095796b`;
- source digest: `178cd8cfb1a859bff411f60e13154109528062cf0ad2384b343d406cc0cc9d61`;
- historical Organic generated mesh: `390 vertices / 570 triangles`;
- historical Organic mesh digest: `d7fc5deaa1c12d1d8c7d7b6dc95bf1e8544ce26140ee2e4a7d2c67a2c4133e48`.

No source point, source radius, flex-zone declaration, leaf, generator, generated mesh, Geometry receiver, Rigging transform, Animation/VFX state, target-host representation, Runtime state or world placement changes in this pass. All seven flex declarations remain `DECLARED_NOT_DEFORMATION_TESTED`.

## Bounded observer

Schema:

`axm.nature-neutral-branch-transition-exit-frame/v0.1`

Scoped state:

`PASS_EXACT_NEUTRAL_BRANCH_TRANSITION_EXIT_FRAMES__CONNECTED_TOPOLOGY_HELD`

The observer consumes the exact existing `axm.nature-neutral-branch-transition-envelope/v0.1` result rather than recomputing a second transition policy. At each first-segment exit boundary it retains:

- exact branch exit center and tapered branch radius;
- exact nearest authored trunk centerline point, trunk segment and local trunk radius;
- normalized first-branch-segment tangent;
- normalized local trunk-segment tangent;
- normalized radial vector from the local trunk centerline to the branch exit center;
- normalized azimuth vector from `cross(trunk_tangent, radial)`;
- the branch tangent decomposed into axial / radial / azimuthal components in that frame;
- branch-vs-local-trunk departure angle;
- orthogonality and reconstruction residuals.

The radial vector is a centerline-derived source-form direction. It is **not** promoted to a production mesh surface normal. The complete frame is not a weld ring, skinning frame, joint frame, biological attachment axis or motion policy.

## Exact current source-form result

All five primary branch exit frames are finite, orthonormal within the observer tolerance and reconstruct each authored first-segment tangent within tolerance.

The current branch-vs-local-trunk departure-angle range is:

- minimum: `44.370330295008024°` (`north-low`);
- maximum: `53.27022259196141°` (`west-high`).

The previously measured transition lengths remain unchanged:

- `south-low`: `0.060865238939307745 m`;
- `north-low`: `0.0645372173379806 m`;
- `east-mid`: `0.021620222932618765 m`;
- `west-high`: `0.036156960642309076 m`;
- `north-top`: `0.021221011737572286 m`.

### North-low exact handoff witness

At the exact existing `north-low` transition boundary:

- nearest trunk segment: `mid->upper`;
- transition parameter: `0.14801958337760968`;
- transition length: `0.0645372173379806 m`;
- branch radius: `0.0518915887490702 m`;
- centerline distance: `0.04875371484283564 m`;
- local trunk radius: `0.10064530359190585 m`;
- departure angle from the local trunk tangent: `44.370330295008024°`.

The authored branch tangent decomposes in the local exit frame as:

- axial along trunk: `0.7148348933173425`;
- radial away from trunk centerline: `0.6944724539616899`;
- azimuthal around trunk: `0.08197003101385525`.

Those values describe the exact neutral source form only. They do not prescribe a shared ring orientation, weld angle, parent frame, bone axis, blend field or deformation direction.

## Reusable handoff rule

`NEUTRAL_TRANSITION_EXIT_FRAME_IS_SOURCE_FORM_EVIDENCE__NOT_WELD_RING_SKINNING_FRAME_OR_JOINT_POLICY`

Operationally:

- Organic owns the authored centerlines/radii and this exact neutral source-form frame.
- Geometry may consume the exact center/radius/tangents only after binding this source identity and must independently choose/verify any connected-junction strategy.
- Rigging retains hierarchy, joints, weights, constraints and deformation acceptance.
- Animation and VFX retain timing/response semantics.
- Technical Art retains target-host transport/representation.
- Runtime retains controller/device/performance behavior.
- Art Direction / Visual QA retain perceptual acceptance.

No downstream PASS transfers merely because the frame is precise.

## Fail-closed controls

The new observer inherits the exact transition owner's failure boundaries and additionally rejects:

- missing or ambiguous branch/trunk identities;
- zero-length source tangents;
- a transition radial distance that disagrees with the exact transition owner;
- a radial vector that is not orthogonal to the selected local trunk segment within tolerance;
- a branch-tangent decomposition that does not reconstruct the authored branch tangent;
- non-finite frame vectors.

Tests also preserve exact `north-low` frame values and refuse promotion of any source flex declaration beyond `DECLARED_NOT_DEFORMATION_TESTED`.

## Visual / structural evidence retention

Source form and generated mesh bytes remain unchanged, so no new visual shape is manufactured. The existing front/side/top wire views and OBJ remain the exact visual evidence for the same source. The evidence build adds `root-transition-exit-frames.json` as structural evidence beside the existing source, mesh, deformation, flex-interaction and root-transition receipts.

## Truth boundary

This result proves only deterministic neutral source-space frame relationships at the five exact authored first-segment radial-envelope exit boundaries.

It does **not** establish:

- a connected or welded production branch/trunk mesh;
- a preferred weld, bridge, boolean, remesh or shared-ring construction;
- a production surface normal or tangent basis;
- botanical correctness, biological attachment mechanics or valid plant ROM;
- tissue strength, stiffness, stress, fatigue or physical flex law;
- Rigging hierarchy, bones, skinning weights or blended deformation;
- physical wind, collision or continuous self-intersection freedom;
- target-host or target-device readiness;
- final Art / QA acceptance;
- CANON;
- production/game readiness;
- Organic, Geometry, Rigging or any specialist mastery.

The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.
