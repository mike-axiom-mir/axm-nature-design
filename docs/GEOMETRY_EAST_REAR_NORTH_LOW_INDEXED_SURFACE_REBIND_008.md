# Geometry — north-low indexed trunk surface rebind 008

## Bounded question

The previous Geometry lane proved an eight-vertex branch-side open ring and projected it to the authored tapered trunk **analytic envelope**. Before that loop can become a real indexed cut, Geometry must prove where the same intended opening lands on the actual generated ten-sided `mid->upper` trunk shell.

This successor therefore reprojects each of the eight branch-ring rays onto the exact decagonal indexed cross-section at its existing axial parameter, then resolves each point to exactly one generated source triangle by barycentric membership. It does not cut, weld, adopt or replace the source mesh.

## Exact owners

- analytic Geometry donor head: `14d05fdabc943376c231308001d00eb87dc23430`;
- analytic bridge module blob: `47ba57110f43b0f3f1f29582b46a90505fa4515c`;
- current Rigging owner head: `efe99261459858636dbe65b16cbe1d5ad2b93a56`;
- exact authored source blob: `fb12b759e1abfd0455bf46fd39a0eba27095796b`.

The branch starts directly from the current Rigging head, which already contains the merged predecessor Geometry lane. `axm-create-me` remains coordination-only.

## Reusable rule

`ANALYTIC_ENVELOPE_HANDOFF_REQUIRES_EXACT_INDEXED_SURFACE_REPROJECTION_BEFORE_CUT_CONNECTIVITY`

An analytic tapered-cylinder/cone envelope is not automatically the indexed polygonal receiver surface. A cut or weld must bind to the actual indexed faces first, otherwise topology work can silently target geometry that the receiver does not contain.

## Structural evidence

The verifier:

1. reruns the prior analytic bridge evidence;
2. reconstructs the exact generated receiver from the unchanged source;
3. intersects each branch-ring radial ray with the exact ten-sided trunk polygon at the same axial parameter;
4. proves each resulting point belongs to exactly one generated side triangle with bounded plane residual and barycentric coordinates;
5. checks the eight-point loop is simple in local trunk parameter space;
6. rebuilds the diagnostic bridge against the indexed-surface loop and rechecks topology and neutral triangle area;
7. measures analytic-envelope versus indexed-surface displacement rather than assuming they are equivalent;
8. rejects promotion to indexed cutting, connected topology, continuous deformation clearance, Rigging transfer, target-host acceptance or Runtime acceptance.

Expected local topology remains deliberately open on the trunk side: combined branch-stub + bridge `25v / 40t / 64e`, one component, one 8-edge boundary; bridge annulus `16v / 16t / 32e`, two 8-edge boundaries.

## Why this is higher leverage than cutting immediately

The exact indexed shell is a decagon, while the predecessor loop was projected to a smooth radial envelope. Their difference is millimetre-scale and materially changes bridge span. A real indexed cut should therefore consume this rebind rather than silently cutting the analytic loop coordinates into a different receiver surface.

## Truth boundary / handoff

This lane proves only neutral source-space membership of the intended eight trunk-side opening points on the exact generated indexed receiver and structural cleanliness of the still-open diagnostic bridge. It does **not** prove an integrated trunk cut, connected/welded branch-trunk topology, continuous motion/foldover/collision freedom, production skinning, Organic source adoption, Rigging transfer, Animation/VFX timing, Technical-Art target-host transport, Runtime/device behavior, gameplay suitability, CANON, production readiness, game readiness or Geometry mastery.

Next Geometry work, if the evidence stays green, is the actual indexed cut/remesh using these exact source-face memberships, followed by a fresh connected-topology and intersection audit. Rigging must separately rebind any articulation proof to that successor.
