# Geometry — north-low attachment topology classification 005

## Why this exists

Rigging PR #14 now has an exact parent-influence exclusion gate for `north-low`. That is a transform-policy result, not evidence that the generated branch is a connected production mesh junction with the trunk. Geometry therefore rebinds the exact current Rigging owner and classifies the indexed topology of the unchanged receiver before any connected-attachment language may transfer downstream.

## Exact identities

- Geometry predecessor: `200ab4b60b8a54460f1265b0ee52eb111f1b280a`
- Rigging owner: `975931b11555d156e04e2ab12e9756fc6c9598a3`
- Organic source owner: `fdc9d2b6ee729728551e22fd3eafa23ad60b6c7a`
- source digest: `178cd8cfb1a859bff411f60e13154109528062cf0ad2384b343d406cc0cc9d61`
- migrated receiver digest: `aa9d450a78fef722672ea9af0f9aca98b4c1a0ca3705661784f5f61f3e9b6a31`

No source geometry or Rigging transform is rewritten.

## Bounded result

`PASS_NORTH_LOW_DIAGNOSTIC_CHILD_TOPOLOGY_CLASSIFIED__HOLD_CONNECTED_BRANCH_TRUNK_ATTACHMENT`

The exact Rigging child partition remains 52 vertices / 72 triangles, but it is not one connected manifold. Indexed edge connectivity resolves six components:

- two closed tapered-segment shells, each 18 vertices / 32 triangles / 48 edges / zero boundary edges / Euler characteristic 2;
- four open leaf-blade components, each 4 vertices / 2 triangles / 5 edges / 4 boundary edges / Euler characteristic 1.

The root branch segment is itself a closed capped shell. The selected `north-low` child and trunk share zero indexed vertices. Therefore this diagnostic receiver does not prove a welded, indexed branch/trunk junction even though Organic can measure spatial support and Rigging can reason about socket transforms.

## Reusable rule

`SPATIAL_OR_RIGGING_ATTACHMENT_EVIDENCE_MUST_DECLARE_INDEXED_CONNECTIVITY_CLASS_BEFORE_CONNECTED_TOPOLOGY_PASS_TRANSFER`

Spatial overlap/support, flex-envelope relationships, socket pivots and transform inheritance are different evidence classes from indexed mesh connectivity. A downstream lane must not convert one into the other silently.

## Why this matters

The current generator intentionally builds tapered segments and leaves as independently authored pieces. That is useful diagnostic/proxy geometry, but the branch root is capped rather than exposing a welded production attachment interface. The correct next production-topology owner, if such a mesh is later requested, must explicitly choose and verify a junction strategy rather than inheriting a false connected-mesh PASS from Rigging.

## Failure controls

The verifier fails closed on Rigging-owner drift, Geometry-predecessor drift, attempted connected-attachment promotion, attempted production-topology promotion, attempted production-skinning acceptance, and attempted Runtime acceptance.

## Explicit non-claims

This evidence does not prove or choose a production weld/boolean/remesh pattern, branch/trunk skinning, botanical tissue continuity, deformation quality, collision, Animation/VFX behavior, target-host transport, Runtime/device behavior, visual quality, gameplay suitability, CANON, production readiness, game readiness, or Geometry mastery.

Truth; Agency / non-domination; Continuity; and Wisdom before speed remain the merge gate.
