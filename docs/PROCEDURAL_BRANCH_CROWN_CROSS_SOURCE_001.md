# Procedural branch/crown cross-source probe 001

Status: **DRAFT / MIGRATED SOURCE-LINEAGE PROCEDURAL EVIDENCE / NOT SOURCE REPLACEMENT**

## Repeated pattern

Nature has three materially different source-owned vegetation studies that share the same authored structural vocabulary: a tapered trunk, explicit branch chains, leaf clusters and the same `axm.nature-organic-form-study/v0.1` evaluator. Re-authoring bounded branch/crown alternatives separately for each source would duplicate the same deterministic mutation mechanics.

The Nature-local mutator therefore remains shared across:

- `sapling-neutral-001`, source origin `fbc202449981f2bac153951c561ed0ed6120c936`;
- `compact-east-tree-neutral-001`, source origin `64116d63fc76daa1623b5fd5046a4e6074100bda`;
- `east-rear-tree-neutral-001`, source origin `a4e5ee011e1d87f47866a7e6c6f4e66f57b6af12`.

The generic v0.2 profile schema is `axm.nature-branch-crown-variation-family/v0.2`. The historical sapling v0.1 schema remains supported so earlier retained evidence stays reproducible.

## Source-lineage transition

Geometry PR #9 migrated the already-proven tapered-cap winding repair into the shared Nature source generator at exact commit:

`4ddbe66e5c02d22407ef773d5346a2fe6f349a2d`

This procedural lane is now stacked directly on that migration. The prior procedural head remains historical evidence:

- exact head `d535484d4c3623a32fc9f5dc44b0856619b9b1ec`;
- retained cross-source artifact `10434505038`;
- archive SHA-256 `7098f308b63bd4b6d8e121255f67378979045d6849abd9c22370bd4d087b9102`.

That artifact is not rewritten or relabelled. It describes the historical mesh lineage.

The current migration rebind deliberately preserves the old procedural authoring inputs:

- `src/axm_nature_design/procedural_family.py` must remain exact Git blob `2e405aa3a7a85228d61cc1f1d3dd76e06aaa2341`;
- all three family-profile files must remain their exact historical Git blobs;
- all nine derived candidate **source digests** must reproduce the prior retained source digests exactly;
- the current `organic_form.py` must be byte-identical to the exact PR #9 migrated donor;
- all nine derived mesh identities must differ from their historical retained mesh identities;
- all nine migrated outputs must report zero indexed shared-edge orientation conflicts;
- every source-specific envelope, branch attachment, immutable-field and Organic structural/design gate remains active;
- every impossible-envelope control must still exhaust exactly three attempts and return `HOLD_NO_VALID_VARIANT`.

This distinguishes a source-generator migration from a procedural parameter rewrite: the branch/crown choices remain the same exact derived source states while the generated topology identity advances truthfully.

## What the mutator may change

Only already-authored branch/crown degrees of freedom:

- branch length scale;
- branch yaw;
- leaf length and width;
- leaf yaw and pitch;
- derived study/procedural provenance and appended truth-boundary text.

Branch attachment roots remain exact. Branch-associated leaf-cluster centers continue to follow their varied branch tips.

Every other top-level source field is treated as immutable automatically rather than by a sapling-specific field list. This includes source-specific fields such as compact/rear `form_intent`, plus trunk, flex zones, design checks, donor provenance and Environment/Weather handoffs.

## Source-specific authority stays local

The mutator is shared, but each source profile retains its own:

- exact source repository/origin ref/path/digest;
- receiving envelope;
- mutation ranges;
- material-difference thresholds;
- failure policy and truth boundary.

The current CI checks out the exact Nature source-generator migration once and uses it as the source/generator donor for all three cases. The procedural branch does not copy Nature form semantics into Universal Creation or Profession Fabric.

## Evidence gate

The migration-rebind builder requires three retained seeds for each of the three source studies: **9 candidate outputs total**. Every candidate must:

- reproduce its historical derived source digest exactly;
- generate a new mesh identity under the migrated Nature generator;
- contain exactly `390` vertices / `570` triangles;
- report zero indexed shared-edge orientation conflicts and zero non-manifold edges;
- preserve `100` boundary edges;
- pass the source Organic Form structural/design checks;
- fit its exact source-specific receiving envelope;
- preserve branch attachment roots;
- preserve all non-authorized top-level source state;
- remain globally distinct from all eight sibling derived outputs.

Each source profile also receives an impossible-envelope three-attempt negative control. It must exhaust all attempts and return `HOLD_NO_VALID_VARIANT`; bounds may not be widened to manufacture a PASS.

## Placement boundary

This is evidence for one reusable **Nature-local** branch/crown mutator rebound to an explicit Nature source-lineage migration. It is not a universal vegetation language, a Geometry repair shim, a Universal Creation mutation rule, or a Profession Fabric source generator.

Map Procedural PR #3 continues to own receiving-scene placement variation. Geometry PR #9 owns the source generator migration. VFX/Rigging retain deformation authority. Environment, Art Direction and Visual QA retain receiving-scene and aesthetic acceptance.

## Non-claims

A PASS does not establish species generation, biological growth, botanical correctness, final visual quality, Environment acceptance, deformation/wind quality, materials/lookdev, global outward-normal correctness for every future generated shape, target runtime fitness, gameplay, CANON, production readiness or Procedural Design mastery.

The four AXM roots remain the merge gate.
